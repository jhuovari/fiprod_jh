# Read ecfin weights from zip file

Weights are in induvidual csv-files.

## Usage

``` r
read_ecfin_weights(zipfile, file_pre)
```

## Arguments

- zipfile:

  A zip-file to read.

- file_pre:

  A first part of the filename inside zip-file.
