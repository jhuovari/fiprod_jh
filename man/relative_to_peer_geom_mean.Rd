% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relative.R
\name{relative_to_peer_geom_mean}
\alias{relative_to_peer_geom_mean}
\title{Relative to peer geometric mean (one column, tidy-eval)}
\usage{
relative_to_peer_geom_mean(
  dat,
  value_col,
  weights,
  by_vars = c("var_id", "measure", "unit_measure", "price_base"),
  out_col = NULL,
  mean_col = NULL
)
}
\arguments{
\item{dat}{Data frame with at least columns \code{time} (Date), \code{geo},
the slice variables in \code{by_vars}, and the value column \code{value_col}.}

\item{value_col}{Unquoted column name in \code{dat} to use as value
(e.g. \code{values}), must be numeric.}

\item{weights}{Weight table with columns:
\code{geo_base}, \code{time} (numeric year), \code{geo} (peer), \code{weight}.}

\item{by_vars}{Character vector: slice variables that must match
between base and peers. Default
\code{c("var_id","measure","unit_measure","price_base")}.}

\item{out_col}{String: name of the output relative column.
Defaults to \code{paste0("rel_", value_col_name)}.}

\item{mean_col}{String: name of the peer-mean column.
Defaults to \code{paste0("geom_mean_peers_", value_col_name)}.}
}
\value{
\code{dat} with two extra columns:
\itemize{
\item \code{mean_col}: weighted geometric mean of peers
\item \code{out_col}: value_col / mean_col
}
}
\description{
For each base observation (time × geo × variable slice), computes one
numeric column relative to the weighted geometric mean of its peers
(other countries) as defined by \code{weights}.
}
\details{
All peers with positive weight must have finite and strictly positive
values; otherwise the peer mean is set to NA for that base observation.
}
\examples{
# relative ULC:
# dat_rel <- relative_to_peer_geom_mean(
#   dat_oecd_pdb_ulc,
#   values,
#   weights_ecfin20,
#   out_col = "ulc_rel"
# )
}
