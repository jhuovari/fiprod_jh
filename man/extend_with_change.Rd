% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extend.R
\name{extend_with_change}
\alias{extend_with_change}
\title{Extend a series forward with growth rates from another source}
\usage{
extend_with_change(x, change, time)
}
\arguments{
\item{x}{A numeric vector of levels or index values, with the missing tail as
\code{NA}.}

\item{change}{A numeric vector of relative changes aligned to \code{x}, e.g.
\code{0.03} for three per cent. Only the values after the last observation of
\code{x} are used.}

\item{time}{A vector of dates or years used to order the series. The result
is returned in the order it was given in.}
}
\value{
A numeric vector as long as \code{x}. \code{NA} where \code{x} was missing and
\code{change} did not reach.
}
\description{
Annual national accounts arrive late for some countries, while a quarterly or
monthly indicator of the same thing is already out. This carries the level of
the annual series forward using the growth of the timelier one, so a chart
does not have to stop a year or two short.
}
\details{
Only the tail is filled: values are written after the last observation of \code{x}
and nothing before or between is touched. The result is an extrapolation, not
a measurement, and is worth marking as such wherever it is shown.
}
\examples{
level  <- c(100, 105, 110, NA, NA)
growth <- c(NA, 0.05, 0.048, 0.05, 0.04)
time   <- 2020:2024

extend_with_change(level, growth, time)

# 110 * 1.05 and then * 1.04
}
\seealso{
\code{\link[=ind_ulc]{ind_ulc()}}, \code{\link[=rebase_index]{rebase_index()}}
}
