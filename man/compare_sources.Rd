% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_sources.R
\name{compare_sources}
\alias{compare_sources}
\title{Compare two data sources on their overlap}
\usage{
compare_sources(
  x,
  y,
  by = c("geo", "activity", "measure", "price_base"),
  time = "time",
  values = "values"
)
}
\arguments{
\item{x, y}{Data frames to compare.}

\item{by}{Character vector of columns identifying a series.}

\item{time}{Name of the time column.}

\item{values}{Name of the value column.}
}
\value{
A data frame with one row per series: the number of shared
observations \code{n}, the median, smallest and largest ratio of \code{x} to \code{y}, and
\code{growth_cor}, the correlation of the yearly relative changes.
}
\description{
Reports how two sources relate on the observations they share. Use it before
\code{\link[=combine_geo_sources]{combine_geo_sources()}} to check that the series are on the same scale (a
median ratio of e.g. 1000 means one source is in millions and the other in
thousands) and that they measure the same thing (a growth correlation well
below one means they do not).
}
\examples{
x <- tibble::tibble(geo = "FI", time = 2018:2020, values = c(100, 110, 120))
y <- tibble::tibble(geo = "FI", time = 2018:2020, values = c(0.1, 0.11, 0.12))
compare_sources(x, y, by = "geo")

}
