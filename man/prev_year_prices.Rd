% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fixed_prices.R
\name{prev_year_prices}
\alias{prev_year_prices}
\alias{fixed_prices}
\title{Convert between fixed and previous year's price series}
\usage{
prev_year_prices(cp, fp, time)

fixed_prices(cp, pyp, time, ref_year = 2020)
}
\arguments{
\item{cp}{A numeric vector of current price values.}

\item{fp}{A numeric vector of fixed price (chain linked volume) values.}

\item{time}{A vector of dates or years. One observation per year is required,
but the years need not be sorted or consecutive.}

\item{pyp}{A numeric vector of previous year's price values.}

\item{ref_year}{Numeric reference (base) year of the returned fixed price
series. Must be present in \code{time}.}
}
\value{
A numeric vector as long as \code{time}.
}
\description{
Chain linked volume series cannot be added together over industries (or any
other breakdown), because every series carries its own chain of price
structures. Series in previous year's prices (PYP) can be added, since within
a year all of them are valued at the same, previous year's, price level.
Aggregating a volume series therefore takes three steps: convert the
components to previous year's prices, add them up, and chain the sum back to
a fixed price series.
}
\details{
\code{prev_year_prices()} does the first step and \code{fixed_prices()} the last one:

\deqn{pyp_t = cp_{t-1} \times fp_t / fp_{t-1}}
\deqn{fp_t = fp_{t-1} \times pyp_t / cp_{t-1}}

\code{fixed_prices()} chains outwards from \code{ref_year} in both directions, so the
result equals \code{cp} in the reference year. The chain stops at a break (a
missing \code{cp} or \code{pyp}, or a gap in \code{time}) and all later (earlier) years are
\code{NA}, because a volume series cannot be linked across a break.

The functions are yearly-data equivalents of \code{statfitools::pp()} and
\code{statfitools::fp()}.
}
\examples{
cp   <- c(100, 120, 150)
fp20 <- c(90, 110, 150)
time <- 2018:2020

pyp <- prev_year_prices(cp, fp20, time)
pyp

# Chaining back reproduces the original volume series
fixed_prices(cp, pyp, time, ref_year = 2020)

}
