% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_sources.R
\name{combine_geo_sources}
\alias{combine_geo_sources}
\title{Combine data sources country by country}
\usage{
combine_geo_sources(..., geos = NULL, geo = "geo", source_col = "source")
}
\arguments{
\item{...}{Named data frames in priority order, e.g.
\code{combine_geo_sources(eurostat = dat_eurostat, oecd = dat_oecd)}. Only the
columns shared by all sources are kept.}

\item{geos}{Optional named list restricting which countries are taken from
which source, e.g. \code{list(eurostat = geo_ea)}. Sources without an entry
contribute every country that no earlier source provided.}

\item{geo}{Name of the country column.}

\item{source_col}{Name of a column recording which source each row came from,
or \code{NULL} to leave it out.}
}
\value{
A data frame with the rows of all sources, each country from one
source only.
}
\description{
Picks each country from the first source that has it. The OECD productivity
database is updated more slowly than Eurostat, so the EU (and EEA) countries
are normally taken from Eurostat and the rest from the OECD.
}
\examples{
eurostat <- tibble::tibble(geo = c("FI", "SE"), time = 2020, values = 1:2)
oecd     <- tibble::tibble(geo = c("FI", "US"), time = 2020, values = 3:4)

# FI comes from Eurostat, US from the OECD
combine_geo_sources(eurostat = eurostat, oecd = oecd)

}
\seealso{
\code{\link[=compare_sources]{compare_sources()}} to check that the sources are on the same scale
before combining them.
}
