% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ulc.R
\name{ind_ulc}
\alias{ind_ulc}
\title{Unit labour cost index}
\usage{
ind_ulc(cost, output, input1 = 1, input2 = 1, time, baseyear)
}
\arguments{
\item{cost}{Compensation of employees, current prices.}

\item{output}{Value added or GDP, chain linked volume.}

\item{input1}{Labour input of the cost, e.g. employees. Defaults to 1, which
leaves the cost as a total.}

\item{input2}{Labour input of the output, e.g. employed persons. Defaults to
1.}

\item{time}{A vector of dates or years.}

\item{baseyear}{Year or years the index is set to 100 in.}
}
\value{
A numeric vector, an index with \code{baseyear} at 100.
}
\description{
Nominal unit labour costs are labour cost per unit of output: the
compensation of employees divided by the volume of output. Both sides can be
put on a per head or per hour basis by giving the labour inputs.
}
\details{
The entrepreneur adjusted measure that the productivity board reports uses
compensation per employee (\code{input1 = employees}) against output per employed
person (\code{input2 = employed}). It scales the cost of employees up to all of
the labour used, which matters in countries where the self-employed are a
large share of employment.

Dividing the compensation in euro rather than in national currency gives the
common currency measure, which moves with the exchange rate as well.
}
\examples{
cost   <- c(100, 104, 110)
output <- c(200, 205, 208)
time   <- 2018:2020

ind_ulc(cost, output, time = time, baseyear = 2020)

# per employee against output per employed person
ind_ulc(cost, output, input1 = c(50, 50, 51), input2 = c(60, 60, 61),
        time = time, baseyear = 2020)

}
\seealso{
\code{\link[=gdp_trading_gain]{gdp_trading_gain()}} for the terms of trade adjusted output,
\code{\link[=rebase_index]{rebase_index()}}, \code{\link[=weight_index2]{weight_index2()}} for the weighting against peers.
}
