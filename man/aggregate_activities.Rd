% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate_activities.R
\name{aggregate_activities}
\alias{aggregate_activities}
\title{Aggregate national accounts industries}
\usage{
aggregate_activities(
  df,
  key,
  ref_year = 2020,
  cp = "V",
  fp = "LR",
  pyp = "Y",
  additive = "_Z",
  activity = "activity",
  price_base = "price_base",
  values = "values",
  time = "time",
  append = TRUE
)
}
\arguments{
\item{df}{A long data frame with an activity column, a price base column, a
time column and a numeric value column. All remaining columns (\code{geo},
\code{measure}, \code{unit_measure}, ...) identify a series and are kept as they are.
Columns that encode the price base themselves (such as the \code{var_id} of the
OECD tables) must be dropped first; the function stops if it finds one.}

\item{key}{A named list mapping each new activity code to its components,
e.g. \code{list(BTNXL = c("BTE", "F", "GTNXL"))}. Components must not overlap
within one aggregate.}

\item{ref_year}{Reference (base) year of the chained fixed price series.}

\item{cp, fp, pyp}{Values of the price base column identifying current price,
fixed price (chain linked volume) and previous year's price series.
Previous year's price series are derived from \code{cp} and \code{fp} when absent.}

\item{additive}{Values of the price base column that may be added up as they
are, such as \code{"_Z"} for employment and hours. Series that are neither
additive nor one of \code{cp}, \code{fp}, \code{pyp} (chain linked indices, ratios such as
value added per hour) cannot be aggregated and are dropped.}

\item{activity, price_base, values, time}{Column names in \code{df}.}

\item{append}{If \code{TRUE} (default) the aggregates are added to \code{df}, replacing
any existing rows for the same activity codes. If \code{FALSE} only the
aggregates are returned.}
}
\value{
A data frame with the same columns as \code{df}.
}
\description{
Builds industry aggregates (such as the OECD business sector \code{BTNXL}, i.e.
NACE B to N excluding L) from a long data frame of national accounts series.
}
\details{
Current price and previous year's price series are simply added up. Chain
linked volumes are not additive, so they are converted to previous year's
prices with \code{\link[=prev_year_prices]{prev_year_prices()}}, added up, and chained back with
\code{\link[=fixed_prices]{fixed_prices()}}. When the data already contains previous year's price series
(Eurostat publishes them, OECD does not) those are used and only the missing
ones are derived from the current and fixed price series.

An aggregate is \code{NA} in every year in which any of its components is missing,
so that a partial sum is never mistaken for the aggregate.
}
\examples{
dat <- tibble::tibble(
  geo        = "FI",
  measure    = "GVA",
  activity   = rep(c("F", "J"), each = 6),
  price_base = rep(rep(c("V", "LR"), each = 3), 2),
  time       = rep(2018:2020, 4),
  values     = c(100, 110, 120,  95, 105, 120,   # construction
                  50,  52,  60,  48,  50,  60)   # information
)

aggregate_activities(dat, list(FJ = c("F", "J")), ref_year = 2020)

}
\seealso{
\code{\link[=prev_year_prices]{prev_year_prices()}}, \code{\link[=combine_geo_sources]{combine_geo_sources()}}
}
